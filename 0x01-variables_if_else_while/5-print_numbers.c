#include <stdio.h>
/**
 * main - entry point
 * Return: 0 ends program
 */
int main(void)
{
	int numbers;

	for (numbers = 0; numbers < 10; numbers++)
	{
		printf("%d", numbers);
	}
	printf("\n");
	return (0);
}
