#include <stdio.h>
/**
 * main - entry point
 * Return: 0 ends program
 */
int main(void)
{
	int numbers;

	for (numbers = 48; numbers <= 57; numbers++)
	{
		putchar(numbers);
	}
	putchar('\n');
	return (0);
}
