#include "main.h"
/**
 * _isdigit - entry point
 * @c: character to be tested for digit value
 * Return: 1 or 0 if character is a digit and otherwise respectively
 */
int _is digit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
