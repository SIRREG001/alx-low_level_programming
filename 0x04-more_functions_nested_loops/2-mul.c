#include "main.h"
/**
 * mul - entry point
 * @a: digit to be multiplied
 * @b: digit to be multiplied
 * Return: the product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
